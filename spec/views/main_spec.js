describe("main view", function() {
  var Main = require("../../src/views/main.js");
  it("needs adam to write the log and channels view specs first", function() {
    pending("because I need to write other specs first");
  });
});
